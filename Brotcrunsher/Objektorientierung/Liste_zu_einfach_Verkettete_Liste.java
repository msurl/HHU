
public class Liste_zu_einfach_Verkettete_Liste {
	
	
	/*Einfach_Verkettete_Listen start;
	
	public Liste_zu_einfach_Verkettete_Liste(){
		start = new Einfach_Verkettete_Listen("Kopf");
	}
	
	public void insertLast(String s){
		Einfach_Verkettete_Listen newEle = new Einfach_Verkettete_Listen(s);
		start.next = newEle;
	}
	
	public String get(int n)
	{
		if(findIndex(n) == null)
		{
			return"";
		}
		return findIndex(n).s;
	}
	
	public void delete(int n)
	{
		if(n < 1)
		{
			return;
		}
		Einfach_Verkettete_Listen nth = findIndex(n);
		if(nth == null)
		{
			return;
		}
		
		Einfach_Verkettete_Listen pre = findIndex(n-1);
		pre.next = nth.next;
		
	}
	
	private Einfach_Verkettete_Listen findIndex(int n){
		if(n < 0)
		{
			return null;
		}
		Einfach_Verkettete_Listen iterator = start;
		for(int i = 0; i < n; i++)
		{
			if(iterator.hasNext())
			{
				iterator = iterator.next;
			}else{
				return null;
			}
		} 
	return iterator;
	}
}
*/
	
}
